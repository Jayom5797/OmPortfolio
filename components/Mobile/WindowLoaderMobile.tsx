import React from 'react';

const WindowLoaderMobile = () => (
  <div style={{
    width: '100%',
    height: '100%',
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    background: 'rgba(0,0,0,0.08)',
    color: '#fff',
    fontSize: 20,
    fontWeight: 500,
    letterSpacing: 1,
  }}>
    Loading...
  </div>
);

export default WindowLoaderMobile;
